This is an example of using kittest with egui.

See [egui_kittest](https://github.com/emilk/egui/tree/main/crates/egui_kittest) for a complete kittest integration.

To run these:
```bash
cargo run -p integration_example --bin basic_integration
cargo run -p integration_example --bin querying
```
